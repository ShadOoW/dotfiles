exec {
    swayidle &
    autotiling-rs &
    wl-paste --watch cliphist store &
    
    # Start applications in background with consistent naming convention
    sp_terminal &
    sp_music &
    sp_explorer &
    
    # Start browser
    vivaldi-stable &
}
