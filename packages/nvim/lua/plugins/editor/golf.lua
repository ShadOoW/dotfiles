return {
    'vuciv/golf',
    cmd = {'GolfStart', 'GolfNext', 'GolfPrev', 'GolfEnd'},
    event = 'VeryLazy',
    config = function()
        -- Optional: Add keymaps or custom config here
    end
}
